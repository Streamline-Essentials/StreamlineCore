package singularity.permissions;

/**
 * Enumerates the types of permission meta-data that the framework
 * recognises and can retrieve via a {@link MetaGrabber}.
 */
public enum MetaKey {

    /** Represents a player's chat/display prefix. */
    PREFIX,

    /** Represents a player's chat/display suffix. */
    SUFFIX,
    ;
}
